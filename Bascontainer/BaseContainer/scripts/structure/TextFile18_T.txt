ALTER TABLE RH_STATUS
ADD CODE CHAR(3);


CREATE GENERATOR GEN_RH_HORAIRES_REEL_SAISIE_id;
SET GENERATOR GEN_RH_HORAIRES_REEL_SAISIE_id TO 1;

ALTER TABLE RH_BASE_HORAIRES_REEL_SAISIE
ADD ID INTEGER;

update RH_BASE_HORAIRES_REEL_SAISIE set ID = GEN_ID(GEN_RH_HORAIRES_REEL_SAISIE_id, 1);


update RDB$RELATION_FIELDS set
RDB$NULL_FLAG = 1
where (RDB$FIELD_NAME = 'ID') and
(RDB$RELATION_NAME = 'RH_BASE_HORAIRES_REEL_SAISIE');

ALTER TABLE RH_BASE_HORAIRES_REEL_SAISIE
ADD CONSTRAINT PK_RH_BASE_HORAIRES_REEL_SAISIE
PRIMARY KEY (ID);

ALTER TABLE UTILISATEUR
ADD UTIL_NUMTELINTERNE VARCHAR(10);

ALTER TABLE BASE_ACCESS
ADD BPTRANSFERT CHAR(1);

ALTER TABLE BASE_ACCESS
ADD BASEPRACT_COMPTABILITE CHAR(1);

ALTER TABLE BASE_ACCESS
ADD BASEPRACT_CANDELETEENCAISSEMENT CHAR(1);

ALTER TABLE BASE_ACCESS
ADD BASEPRACT_CANDELETEACTE CHAR(1);

CREATE TABLE BASE_POINTAGE (
    ID              INTEGER NOT NULL,
    DATEPOINTAGE    TIMESTAMP NOT NULL,
    ID_UTILISATEUR  INTEGER NOT NULL,
    ENTRESORTIE     SMALLINT NOT NULL
);


ALTER TABLE BASE_POINTAGE ADD CONSTRAINT PK_BASE_POINTAGE PRIMARY KEY (ID);


