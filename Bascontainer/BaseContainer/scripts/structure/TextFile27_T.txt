ALTER TABLE BASE_PAIEMENTREEL
ADD INCN CHAR(5);

ALTER TABLE PERSONNE
ADD AUTREPROFESSION VARCHAR(50);


CREATE TABLE BAS_SUIVISPECIALISTE (
    ID INTEGER NOT NULL,
    ID_PATIENT INTEGER NOT NULL,
    DATE_ENVOIS TIMESTAMP NOT NULL,
    ID_CORRESPONDANT INTEGER NOT NULL,
    COMMENTAIRES VARCHAR(1000));

ALTER TABLE BAS_SUIVISPECIALISTE
ADD CONSTRAINT PK_BAS_SUIVISPECIALISTE
PRIMARY KEY (ID);

CREATE TABLE BASE_HISTORESP (
    DATEEVENT TIMESTAMP,  
    USEREVENT INTEGER,
    IDPATIENT                INTEGER NOT NULL,
    ASSISTANTE_RESP          INTEGER,
    PRATICIEN_RESP           INTEGER,
    PRATICIEN_UNIQUE         CHAR(5)
);

