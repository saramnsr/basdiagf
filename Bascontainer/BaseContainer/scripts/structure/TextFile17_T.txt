ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD TNLDROIT SMALLINT;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD TNLGAUCHE SMALLINT;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD LISTOFPOINTSANOCCD BLOB SUB_TYPE 0 SEGMENT SIZE 4096;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD LISTOFPOINTSANOCCF BLOB SUB_TYPE 0 SEGMENT SIZE 4096;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD LISTOFPOINTSANOCCG BLOB SUB_TYPE 0 SEGMENT SIZE 4096;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD LISTOFPOINTSSOURIRE BLOB SUB_TYPE 0 SEGMENT SIZE 4096;

ALTER TABLE BASEDIAG_DIAGNOSTIC
ADD CONTROLE VARCHAR(200);

CREATE TABLE BASEDIAG_PLANTRMNT (
    ID_DIAG INTEGER NOT NULL,
    CTRLKEY CHAR(40) NOT NULL,
    RESOURCENAME VARCHAR(40) NOT NULL,
    X1 DOUBLE PRECISION NOT NULL,
    Y1 DOUBLE PRECISION NOT NULL,
    X2 DOUBLE PRECISION NOT NULL,
    Y2 DOUBLE PRECISION NOT NULL);

ALTER TABLE BAS_COMMONOBJECTIF
ADD CATEGORIE SMALLINT;





ALTER TABLE BASE_RECONTACT
ADD MOYEN SMALLINT NOT NULL;

ALTER TABLE BASE_RECONTACT
ADD MOYENPROCHAINETENTATIVE SMALLINT NOT NULL;

UPDATE base_recontact
set BASE_RECONTACT.moyen = 0,BASE_RECONTACT.moyenprochainetentative = -1
where BASE_RECONTACT.moyen is null;

UPDATE base_recontact
set BASE_RECONTACT.moyenprochainetentative = 0
where BASE_RECONTACT.moyenprochainetentative = -1 and (BASE_RECONTACT.numtentative<2 or BASE_RECONTACT.numtentative is null) ;

UPDATE base_recontact
set BASE_RECONTACT.moyenprochainetentative = 3
where BASE_RECONTACT.moyenprochainetentative =-1 and BASE_RECONTACT.numtentative>=2;

ALTER TABLE BASE_RECONTACT DROP NUMRECORD;

ALTER TABLE TYPE_PERS
ADD DISPLAYORDER SMALLINT DEFAULT 0;




