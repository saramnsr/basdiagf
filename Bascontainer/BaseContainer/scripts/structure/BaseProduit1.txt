CREATE TABLE BASE_LABO_ELEMENTFACTURABLE (
    ID INTEGER NOT NULL,
    DATEELEMENT TIMESTAMP NOT NULL,
	CODEARTICLE VARCHAR(10) NOT NULL,
	PRIXHT	DOUBLE PRECISION NOT NULL,
    PRIXTTC	DOUBLE PRECISION NOT NULL,
    Quantite INTEGER NOT NULL,
    TVA	DOUBLE PRECISION NOT NULL,
    LIBELLE VARCHAR(200) NOT NULL);

ALTER TABLE BASE_LABO_ELEMENTFACTURABLE
ADD CONSTRAINT PK_BASE_LABO_ELEMENTFACTURABLE
PRIMARY KEY (ID);

