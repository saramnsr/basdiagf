ALTER TABLE UTILISATEUR
ADD GOOGLE_LOGIN VARCHAR(100);

ALTER TABLE UTILISATEUR
ADD GOOGLE_PASSWD VARCHAR(100);

ALTER TABLE UTILISATEUR
ADD GOOGLE_LASTSYNCHRO TIMESTAMP;

--    <add key="PrefixeFactSociete" value="01"/>

update RDB$FIELDS set
RDB$FIELD_TYPE = 37,
RDB$FIELD_LENGTH = 20,
RDB$CHARACTER_LENGTH = 20
where RDB$FIELD_NAME = 'RDB$318';

-- Table BANQUE colonne NUMCPT_BAN passé en varchar(20)