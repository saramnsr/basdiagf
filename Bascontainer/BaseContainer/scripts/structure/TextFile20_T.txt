ALTER TABLE TYPE_PERS
ADD CATEGORIE VARCHAR(10);

CREATE TABLE BAS_PNF_CHECK (
    ID INTEGER NOT NULL,
    ID_PART_PATIENT INTEGER,
    ID_PART_BANQUE INTEGER,
    "TYPE" VARCHAR(10),
    "DATE" TIMESTAMP,
    LIBELLE VARCHAR(100),
	MONTANT_TOTAL DOUBLE PRECISION);

ALTER TABLE BAS_PNF_CHECK
ADD CONSTRAINT PK_BAS_PNF_CHECK
PRIMARY KEY (ID);

