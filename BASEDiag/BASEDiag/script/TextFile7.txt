CREATE TABLE BASE_PRINTERSETTINGS (
    LIBELLE VARCHAR(200) NOT NULL,
    DESCRIPTIF VARCHAR(2000) NOT NULL,
    SETTINGS BLOB SUB_TYPE 0 SEGMENT SIZE 80);
    
    
alter table BASE_PRINTERSETTINGS
add constraint PK_BASE_PRINTERSETTINGS
primary key (LIBELLE);

