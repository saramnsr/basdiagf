CREATE TABLE BASE_SEMESTRE (
    ID INTEGER NOT NULL,
    ID_TRAITEMENT INTEGER NOT NULL,
    CODESEMESTRE VARCHAR(5) NOT NULL,
    ID_ACTE_GESTION INTEGER NOT NULL,
    MONTANT_SEMESTRE NUMERIC(15,2) NOT NULL,
    NB_SURVEILLANCE INTEGER NOT NULL,
    ID_ACTE_GESTION_SURV INTEGER,
    MONTANT_SURVEILLANCE NUMERIC(15,2),
    DATEDEBUT TIMESTAMP,
    DATEFIN TIMESTAMP);
    
alter table BASE_SEMESTRE
add constraint PK_BASE_SEMESTRE
primary key (ID);

ALTER TABLE BASE_SEMESTRE
ADD NUMSEMESTRE SMALLINT;



CREATE TABLE BASE_POSE_APPAREIL (
    ID INTEGER NOT NULL,
    ID_PROPOSITION INTEGER NOT NULL,
    ID_APPAREIL INTEGER NOT NULL);
    
alter table BASE_POSE_APPAREIL
add constraint PK_POSE_APPAREIL
primary key (ID);


CREATE TABLE BASE_POSE_APP_SEMESTRE (
    ID_POSE_APPAREIL INTEGER NOT NULL,
    ID_SEMESTRE INTEGER NOT NULL);
    
alter table BASE_POSE_APP_SEMESTRE
add constraint PK_POSE_APP_SEMESTRE
primary key (ID_POSE_APPAREIL,ID_SEMESTRE);

ALTER TABLE BASE_PLAN_TRAITEMENTS
ADD PHASE SMALLINT;
