ALTER TABLE BASEDIAG_INFOCOMPLEMENTAIRE
ADD AMELIORATIONS BLOB SUB_TYPE 1 SEGMENT SIZE 4096 CHARACTER SET ISO8859_1 ;


ALTER TABLE BASEDIAG_INFOCOMPLEMENTAIRE
ADD DEBUTTRAITEMENTENVISAGE TIMESTAMP ;

DROP TABLE BASE_DIAG_PHASE;

CREATE TABLE BASE_DIAG_PHASE (
    ID INTEGER NOT NULL,
    ID_PROPOSITON INTEGER NOT NULL,
    DUREE INTEGER NOT NULL,
    TARIFSEMESTRE double precision NOT NULL,
    ID_BASE_ACTE_GESTION INTEGER NOT NULL,
    TYPEDEPHASE VARCHAR(20));
    
alter table BASE_DIAG_PHASE
add constraint PK_PHASE
primary key (ID);
